
(require 'package)
(add-to-list 'package-archives '("melpa" . "https://melpa.org/packages/") t)


(package-initialize)

(add-to-list 'load-path "~/.emacs.d/lisp/config/")
(add-to-list 'load-path "~/.emacs.d/lisp/plugins/")

(unless package-archive-contents
  (package-refresh-contents))

(require 'common)
(require 'modeline)
(require 'lsp)
(require 'helpers)
(require 'theme)
(require 'dst by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 '(package-selected-packages
   '(orderless magit mood-line company consult lsp lsp-mode lsp-ui use-package vertico)))
(custom-set-faces
 ;; custom-set-faces was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 )
