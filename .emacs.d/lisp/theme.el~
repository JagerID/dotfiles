;; Тема
;; (add-to-list 'custom-theme-load-path "~/.config/doom/themes/ember/")
;; (load-theme 'ember t)

(use-package doom-themes
  :config
  (load-theme 'doom-one t))

(defun customize-theme ()
  (custom-set-faces
   '(font-lock-keyword-face ((t (:background nil :slant italic))))))

(add-hook 'prog-mode-hook #'customize-theme)
